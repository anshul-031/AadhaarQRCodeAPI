services:
  - type: web
    name: aadhaar-qr
    runtime: docker
    region: singapore
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: PYTHON_VERSION
        value: 3.11.0
    healthCheckPath: /
    autoDeploy: true